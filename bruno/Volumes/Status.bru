meta {
  name: Status
  type: http
  seq: 4
}

patch {
  url: {{api_url}}/volumes/:code
  body: json
  auth: none
}

params:path {
  code: 1
}

headers {
  Authorization: Bearer {{bearer_token}}
}

body:json {
  {
    "status": "RETURNED"
  }
}
