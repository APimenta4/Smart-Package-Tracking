meta {
  name: Email
  type: http
  seq: 1
}

post {
  url: {{api_url}}/users/:code/email
  body: json
  auth: none
}

params:path {
  code: client
}

headers {
  Authorization: Bearer {{bearer_token}}
}

body:json {
  {
    "subject": "Meeting Reminder",
    "body": "this is a test email heheh"
  }
  
}
